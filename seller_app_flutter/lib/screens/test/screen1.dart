import 'package:flutter/material.dart';

class MyTestOne extends StatelessWidget {
  const MyTestOne({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('One')),
    );
  }
}
